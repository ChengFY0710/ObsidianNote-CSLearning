## 01 printf的 * 修饰符

```c
double weight = 123.456;
int width=5,precision=2;
printf("The number is %*.*f.",width,precision,weight);
```
星号 * 相当于一个占位。

## 02 scanf的 * 修饰符
将 * 放在% 和 d之间，会使scanf跳过相应的输入项。
```c
scanf("%*d %*d %d",&n);
printf("%d",n);
```
输入 34 56 78 最后输出78
