编译提示：GCC/Clang 编译时需加 `-lm` 链接数学库（如 `gcc code.c -o code -lm`）；  

宏常量：`M_PI`（π）、`M_E`（自然常数e）需开启 **C99及以上标准** 或定义 `_GNU_SOURCE` 才能使用。

## 一、基本运算（高频）

| 函数名               | 核心功能                        | 参数/注意事项               | 示例（返回值）               |
| ----------------- | --------------------------- | --------------------- | --------------------- |
| `sqrt(double x)`  | 计算 ( \sqrt{x} )（平方根）        | ( x \geq 0 )，否则返回NaN  | `sqrt(4.0)` → 2.0     |
| `pow(double x,y)` | 计算 ( x^y )（幂运算）             | x、y为浮点数（如2.0^3=8.0）   | `pow(2.0, 3.0)` → 8.0 |
| `fabs(double x)`  | 计算 ( \vert x \vert )（浮点绝对值） | 处理double类型，整型用`abs()` | `fabs(-3.5)` → 3.5    |
| `cbrt(double x)`  | 计算 ( \sqrt[3]{x} )（立方根）     | 支持负数（如-8的立方根为-2）      | `cbrt(-8.0)` → -2.0   |
## 二、指数与对数

|函数名|核心功能|参数/注意事项|示例（返回值）|
|---|---|---|---|
|`exp(double x)`|计算 ( e^x )（自然指数）|e≈2.718，x为任意double|`exp(1.0)` → ~2.718|
|`log(double x)`|计算 ( \ln x )（自然对数）|( x > 0 )，否则返回NaN|`log(M_E)` → 1.0|
|`log10(double x)`|计算 ( \log_{10}x )（常用对数）|( x > 0 )|`log10(100.0)` → 2.0|
|`log2(double x)`|计算 ( \log_2x )（2为底对数）|C99新增，( x > 0 )|`log2(8.0)` → 3.0|
## 三、三角函数（弧度制！）

注意：所有三角函数参数均为 **弧度**，如需处理角度，需先转换（弧度 = 角度 × π/180）。

|函数名|核心功能|参数/注意事项|示例（返回值）|
|---|---|---|---|
|`sin(double x)`|正弦 ( \sin x )|x为弧度（如30°→π/6）|`sin(M_PI/2)` → 1.0|
|`cos(double x)`|余弦 ( \cos x )|x为弧度（如90°→π/2）|`cos(M_PI)` → -1.0|
|`tan(double x)`|正切 ( \tan x )|避免x=π/2 + kπ（正切无意义）|`tan(M_PI/4)` → 1.0|
|`atan2(double y,x)`|象限反正切 ( \arctan(y/x) )|自动判断象限（比`atan`更实用）|`atan2(1,0)` → π/2|
## 四、取整函数（实用）

|函数名|核心功能|特点（区别于其他取整）|示例（返回值）|
|---|---|---|---|
|`ceil(double x)`|向上取整（≥x的最小整数）|无论小数部分多大，均向上进1|`ceil(2.1)` → 3.0；`ceil(-2.1)` → -2.0|
|`floor(double x)`|向下取整（≤x的最大整数）|无论小数部分多大，均向下舍1|`floor(2.9)` → 2.0；`floor(-2.9)` → -3.0|
|`round(double x)`|四舍五入到最近整数|C99新增，遵循“四舍五入”规则|`round(2.4)` → 2.0；`round(2.5)` → 3.0|
|`trunc(double x)`|截断小数（保留整数部分）|直接丢弃小数，不四舍五入|`trunc(2.9)` → 2.0；`trunc(-2.9)` → -2.0|
## 五、绝对值与最值（补充）

|函数名|核心功能|适用场景|示例（返回值）|
|---|---|---|---|
|`abs(int x)`|整型绝对值（`<stdlib.h>`）|处理int类型（如-5→5）|`abs(-5)` → 5|
|`fmax(double x,y)`|返回两数中的较大值|C99新增，比`if-else`简洁|`fmax(3.2, 5.1)` → 5.1|
|`fmin(double x,y)`|返回两数中的较小值|C99新增，避免手动比较|`fmin(3.2, 5.1)` → 3.2|
## 六、其他常用（避免踩坑）

|函数名|核心功能|为什么实用？|示例（返回值）|
|---|---|---|---|
|`hypot(double x,y)`|计算 ( \sqrt{x^2 + y^2} )（勾股定理）|避免直接计算 ( x^2 ) 溢出|`hypot(3.0,4.0)` → 5.0|
|`fmod(double x,y)`|计算浮点数余数（x mod y）|处理小数余数（如5.5 mod 2→1.5）|`fmod(5.5, 2.0)` → 1.5|
如果需要针对某个函数的 **详细用法示例**（比如如何用`atan2`计算角度、如何处理`sqrt`的非法输入），可以随时告诉我，我会补充具体代码！
> （注：文档部分内容可能由 AI 生成）