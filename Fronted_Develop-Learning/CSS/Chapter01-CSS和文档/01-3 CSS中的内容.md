## 01 CSS 样式的基本结构
由**两个基本部分**构成：**选择符**（selector）和**声明块**（declaration block）。声明块由一个或多个声明组成，而一个声明包含一个属性和对应的值。
![[引用文件/CSS/01-3-1.png]]
+ **选择符**：定义文档哪部分受影响。（如果选择符是 p，那么选择的则是所有 p 元素）
+ **声明块**：由**一个或多个声明**组成。一个声明包含一个CSS属性及其值。
