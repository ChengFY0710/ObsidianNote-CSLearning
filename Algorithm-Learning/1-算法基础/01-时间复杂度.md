## 01 什么是时间复杂度
时间复杂度是一个==**函数**==，**定性地描述该算法的运行时间**。
通过估算**算法的操作单元数量**来**代表程序消耗的时间**，这里默认CPU的每个单元运行消耗的时间都是相同的。
假设算法的问题规模为n，那么**操作单元数量**便用函数f(n)来表示，随着数据规模n的增大，算法执行时间的增长率和f(n)的增长率相同，这称作为算法的**渐近时间复杂度**，简称时间复杂度，记为 **O(f(n))**。

### （1）什么是 O
O 用来表示**上界**。当用它作为算法的**最坏情况运行时间的上界**，就是对任意数据输入的运行时间的上界。但一个默认规定是，O只是代表一般情况，而**不是严格的上界**。![[1-01-1.png]]
## 02 不同数据规模的差异
如下图中可以看出不同算法的时间复杂度在不同数据输入规模下的差异。
![[1-01-2.png]]
在决定使用哪些算法的时候，不是时间复杂度越低的越好，**要考虑数据规模**。如果数据规模很小甚至可以用O(n^2)的算法比O(n)的更合适。

>[!tip]
> 我们说的时间复杂度都是**省略常数项==系数==**的，是因为一般情况下都是**默认数据规模足够的大**，基于这样的事实，给出的算法时间复杂度的一个排行如下所示：
>
> **O(1)常数阶 < O(logn)对数阶 < O(n)线性阶 < O(nlogn)线性对数阶 < O(n^2)平方阶 < O(n^3)立方阶 < O(2^n)指数阶**
>
> 但是也要注意大常数，如果这个常数非常大，例如10^7 ，10^9 ，那么常数就是不得不考虑的因素了。

## 03 复杂表达式的化简
```
O(2*n^2 + 10*n + 1000)
```
首先去掉运行时间中的**加法常数项**。
```
O(2*n^2 + 10*n)
```
再去掉常数系数
```
O(n^2 + n)
```
只保留最高项，去掉数量级小一级的 n，最终简化为：
```
O(n^2)
```

## 04 O(logn) 中的log是以什么为底？
可以是2，可以是10，也可以是20，但我们统一说logn，也就是忽略底数的描述。
![[1-01-3.png]]
