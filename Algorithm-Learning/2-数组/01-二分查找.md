![2-01-1.jpg|1116](/引用文件/Algorithm-Learning/2-01-1.jpg)
## 前提条件
+ 数组为**有序**数组
+ 数组中**无重复**元素

>[!warning]
>若数组中有重复元素，使用二分查找法返回的元素就**不是唯一的**了。

## 二分法的两种写法

**区间的定义**就是不变量，那么在循环中坚持根据查找区间的定义来做边界处理，就是**循环不变量规则**。
### （1）第一种写法
第一种写法，我们定义 target 是在一个在左闭右闭的区间里，**也就是[left, right]（这个很重要非常重要）**。
```cpp
left = 0;
right = numsize - 1;
while(left <= right){
	mid = (left + right)/2;
	if(num[mid] > target){
		right = mid - 1;
	}
	else if(num[mid] < target){
		left = mid + 1;
	}
	else return mid;
}
```
区间的定义这就决定了二分法的代码应该如何写，**因为定义target在[left, right]区间，所以有如下两点：**

- while (left <= right) 要使用 <= ，因为left == right是有意义的，所以使用 <=
- if (nums[middle] > target) right 要赋值为 middle - 1，因为当前这个nums[middle]一定不是target，那么接下来要查找的左区间结束下标位置就是 middle - 1

![2-01-2.jpg](/引用文件/Algorithm-Learning/2-01-2.jpg)
### （2）第二种写法
如果说定义 target 是在一个在左闭右开的区间里，也就是【left, right) ，那么二分法的边界处理方式则截然不同。
```cpp
left = 0;
right = numsize;
while(left < right){
	mid = (left + right)/2;
	if(num[mid] > target){
		right = mid;
	}
	else if(num[mid] < target){
		left = mid;
	}
	else return mid;
}
```
有如下两点：

- while (left < right)，这里使用 < ,因为left == right在区间【left, right)是没有意义的
- if (nums[middle] > target) right 更新为 middle，因为当前nums[middle]不等于target，去左区间继续寻找，而寻找区间是左闭右开区间，所以right更新为middle，即：下一个查询区间不会去比较nums[middle]

![2-01-3.jpg](/引用文件/Algorithm-Learning/2-01-3.jpg)

## 题目链接

https://leetcode.cn/problems/binary-search/